# Copy to .env.local for local dev. In Vercel, set NEXT_PUBLIC_API_URL as a project
# environment variable pointing at wherever apps/api is deployed (Render/Railway/Fly/etc).
NEXT_PUBLIC_API_URL=http://localhost:8000
