[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tikdrop"
version = "0.1.0"
description = "TikTok Made Me Buy It - Portugal: Product Intelligence engine (scoring + profit)"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0,<3.0",
    "sqlalchemy>=2.0",
    # greenlet>=3.2 has no prebuilt wheel for some Python/platform combos (e.g. cp39-win_amd64)
    # and fails to build from source without a C compiler - pin below that.
    "greenlet<3.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
groq = [
    "httpx>=0.27",
]
ingestion = [
    "httpx>=0.27",
    "pytrends>=4.9",
]
api = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "psycopg[binary]>=3.1",
]
all = [
    "tikdrop[dev,groq,ingestion,api]",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
